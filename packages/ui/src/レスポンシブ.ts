// スマホ・タブレットでの狭幅レイアウト判定に使う共通しきい値。
// LAN内のスマホ・タブレットからの閲覧が主用途のため（DESIGN.md 5章 確定済み判断2）、
// 768px未満を「狭幅」として扱う。初期表示の分岐（アプリシェル部品）とCSSメディアクエリ
// （各style.css.ts）の双方がこの1箇所だけを参照することで、しきい値の食い違いを防ぐ。
export const 狭幅ブレークポイントpx = 768;

// Vanilla Extractの style() 内 "@media" キーで使う狭幅判定クエリ文字列
export const 狭幅メディアクエリ = `screen and (max-width: ${狭幅ブレークポイントpx - 1}px)`;
